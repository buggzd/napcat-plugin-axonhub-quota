{
  "name": "napcat-plugin-axonhub-quota",
  "plugin": "AxonHub 订阅额度",
  "version": "1.2.1",
  "description": "按需查询 AxonHub 中 Codex 订阅窗口的轻量 NapCat 插件",
  "type": "module",
  "main": "index.mjs",
  "license": "MIT",
  "napcat": {
    "minVersion": "4.14.0",
    "tags": [
      "工具"
    ]
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node --experimental-strip-types --test test/*.test.ts",
    "build": "node scripts/build.mjs",
    "deploy": "node scripts/deploy.mjs",
    "verify:live": "node scripts/verify.mjs"
  },
  "devDependencies": {
    "@resvg/resvg-wasm": "^2.6.2",
    "@types/node": "^22.0.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.9.0"
  },
  "author": "buggzd"
}
